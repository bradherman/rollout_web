<div class="row" id="flags">
  <input class="search" placeholder="Search" />
  <button class="sort" data-sort="name">
    Sort by name
  </button>

  <ul class="list">
    <% @flags.each_with_index do |flag, index| %>
      <%= render 'flag', flag: flag, index: index %>
    <% end %>
  </ul>
</div>